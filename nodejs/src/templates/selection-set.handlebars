{{#each this}}
{{ log "        property: " (convertedType this) " " (toBetterPascalCase name) level="debug"}}
{{ toCsharpComment description }}
[JsonProperty("{{ name }}")]
public {{{ convertedType this }}} {{ toBetterPascalCase name }} { get; set; }
{{/each}}
