{{#each this}}
{{#if isSelected}}
[JsonProperty("{{ name }}")]
public {{{ typeName }}} {{ toBetterPascalCase name }} { get; set; }
{{/if}}
{{/each}}

{{#each this}}
{{#if isSelected}}
{{#if isArray}}
Option<{{{ interfaceTypeName }}}> I{{ toBetterPascalCase schemaBaseTypeName }}.{{ toBetterPascalCase name }} => Option.Some({{ toBetterPascalCase name }}.Select(i => ({{{ itemName }}})i));
{{else}}
Option<{{{ interfaceTypeName }}}> I{{ toBetterPascalCase schemaBaseTypeName }}.{{ toBetterPascalCase name }} => Option.Some<{{{ interfaceTypeName }}}>({{ toBetterPascalCase name }});
{{/if}}
{{else}}
Option<{{{ interfaceTypeName }}}> I{{ toBetterPascalCase schemaBaseTypeName }}.{{ toBetterPascalCase name }} => Option.None<{{{ interfaceTypeName }}}>();
{{/if}}
{{/each}}