
    {{ toCsharpComment description }}
    public class {{ toBetterPascalCase name }}
    {
{{#each fields}}
      {{ toCsharpComment description }}
      [JsonProperty("{{ name }}")]
      public {{{ convertedType this false }}} {{toBetterPascalCase name }} { get; set; } 
{{/each}}
    }
