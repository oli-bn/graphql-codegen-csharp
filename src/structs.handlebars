

    {{ toCsharpComment description }}
    public struct {{ name }}
    {
{{#each fields}}
      {{ toCsharpComment description }}
      [JsonProperty("{{ name }}")]
      public {{{ convertedType this }}} {{toPascalCase name }} { get; set; } 
{{/each}}
    }
