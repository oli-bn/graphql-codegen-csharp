    {{ toCsharpComment description }}
    [JsonConverter(typeof(StringEnumConverter))]
    public enum {{ toBetterPascalCase name }} 
    {
{{#each values }}
        {{value}}{{#unless @last}}, {{/unless}}
{{/each}}
    }
