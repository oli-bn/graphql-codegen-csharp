using System;
using System.Collections.Generic;
using Newtonsoft.Json;
using Newtonsoft.Json.Converters;

#region input types

namespace Generated
{

// scalars if used 
{{#each (getTypesIfUsed inputTypes scalars "scalars")}}
    {{~> classes }}

{{/each}}

// types if used 
{{#each (getTypesIfUsed inputTypes types)}}
  {{~> classes }}

{{/each}}

// input types
{{#each inputTypes}}
  {{~> classes }}

{{/each}}

// enums if used 
{{#each (getTypesIfUsed inputTypes enums)}}
  {{~> enumTemplate }}

{{/each}}

}

#endregion 

