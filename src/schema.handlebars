using System;
using System.Collections.Generic;
using Newtonsoft.Json;
using Newtonsoft.Json.Converters;

#region input types

namespace Generated
{

    // scalars if used 
{{#each scalars}}
    {{~> structs }}

{{/each}}

    // types if used 
{{#each types }}   
    {~> classes }}

{{/each}}

    // input types
{{#each inputTypes }}   
  {{~> classes }}

{{/each}}

    // enums if used 
{{#each enums}}
  {{~> enumTemplate }}

{{/each}}

}

#endregion 

