using System;
using System.Collections.Generic;
using Newtonsoft.Json;
using Newtonsoft.Json.Converters;

#region input types

namespace Generated
{

// scalars if used 
{{#each (getTypesIfUsed inputTypes operations scalars "scalars")}}
    {{~> structs }}

{{/each}}

// types if used 
{{#each operations }}   
  {{#each (getTypeIfUsed innerModels ../types)}}
    {{~> classes }}
  {{/each}}
{{/each}}

{{#each (getTypesIfUsed inputTypes operations types "types")}}
  {{~> classes }}

{{/each}}

// input types
{{#each inputTypes}}
  {{~> classes }}

{{/each}}

// enums if used 
{{#each (getTypesIfUsed inputTypes operations enums)}}
  {{~> enumTemplate }}

{{/each}}

}

#endregion 

